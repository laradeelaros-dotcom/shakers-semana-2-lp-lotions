<h1>{{ page.title }}</h1>
<p>{{ page.content }}</p>

{% assign collection = collections['magic'] %}

<h2>{{ collection.title }}</h2>
<h3>{{ collection.description }}</h3>


{% schema %}
  {
    "name": "lotion-lp",
    "settings": [],
    "presets": [
      {
        "name": "lotion-lp"
      }
    ]
  }
{% endschema %}